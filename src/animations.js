window.initAnimations = function initAnimations() { if (window.matchMedia('(prefers-reduced-motion: reduce)').matches) return; gsap.registerPlugin(ScrollTrigger); gsap.from('.site-header', { opacity: 0, y: -8, duration: .4 }); gsap.utils.toArray('[data-reveal]').forEach((el) => gsap.from(el, { opacity: 0, y: 18, duration: .6, scrollTrigger: { trigger: el, start: 'top 86%', once: true } })); };
